{
  "name": "dynamodb-crud-api-gateway",
  "version": "1.0.0",
  "description": "use Serverless to deploy API Gateway + Lambda + DynamoDB",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.18.0",
    "@aws-sdk/util-dynamodb": "^3.18.0",
    "aws-lambda": "^1.0.7",
    "uuidv4": "^6.2.13"
  },
  "devDependencies": {
    "serverless-iam-roles-per-function": "^3.2.0",
    "serverless-plugin-typescript": "^2.1.5",
    "typescript": "^5.3.3",
	"@types/aws-lambda": "^8.10.80"
  }
}
